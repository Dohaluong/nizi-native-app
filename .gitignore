# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# Licensed/purchased LUT preset packs — reference material only, never redistributed via git.
# The individual .cube files actually embedded in the app live under
# Nizi/Features/PhotoEditor/Infrastructure/ instead (tracked normally) — see
# docs/modules/PHOTO-EDITOR-PRESET-GUIDE.md § 9 and ADDEDUM.md § 3.1, § 14.
docs/modules/photo-editor/*.zip
docs/modules/photo-editor/LUTs/
